# Anicel — 작업 규칙

> **어기면 사고가 나는 것만** 담는다. 상세·근거·이력은 메모리와 보드가 갖는다.
> ⚠️**짧게 유지할 것.** 한 줄 늘리고 싶으면 **대신 뺄 줄을 먼저 찾는다.**
> ★**넣을 기준은 「유저가 몇 번이나 반복해서 말한 절대명령인가」** — 한 번 나온 이야기는 여기가 아니다.

## 언어

대화·문서는 **한국어**. 코드·주석·커밋 메시지·PR 본문은 **영어**.

## 절대명령 (유저가 반복해서 지시한 것)

1. **통일은 로직 단계부터, 같은 코드로.**
   착수 0수 = 「이 법이 이미 어딘가에 쓰여 있나」 grep하고 **지켜지는 한 곳을 먼저 읽는다.**
   증상 명단만 이행하면 미완주 — **전 가족 × 전 인터랙션** 매트릭스를 닫아야 끝이다.
2. **선택범위는 레이어 불문 자유롭게.** 행의 종류로 막지 않는다.
3. **편의 규칙을 발명하지 말 것.** 「~는 제외한다」 코드는 **누가 시켰는지 못 대면 버그다**
   (`git log -S`로 출처를 확인한다). **한 플래그가 두 질문에 답하는 것도 발명이다.**
4. **원래 있는 걸 조사하고 만들 것.** 새 규칙·새 패널·새 상태를 만들기 전에 기존 것을 찾는다.

## 엉클밥 규칙

**클린 코드 + SOLID + 클린 아키텍처 + 순환 복잡도** 넷.

- **순서**: 전면 감사로 기존 코드를 맞춘 **뒤에** 강제한다. **규칙별로 따로 졸업**시킨다 —
  넷을 한 번에 켜려 하면 마지막 하나가 나머지를 영영 막는다.
- ⛔**「주석은 실패다」는 적용하지 않는다.** 설명 주석은 지우되,
  **결정 주석(누가 왜 그렇게 정했는지)은 절대 지우지 않는다.** 그게 없으면 옛 결정을 버그로 알고 고친다.
- ⛔**복잡도 상한 숫자를 미리 정하지 않는다.** 정하면 감사가 숫자 맞추기가 되고,
  점수만 낮추고 이해도는 떨어지는 분할이 나온다.
- 지금 켜져 있는 규칙 = **의존 방향** 하나.

## 의존 방향 (CI가 강제 중)

`core` · `models` · `services` · `controllers` 는
**`ui/` 도, 위젯 프레임워크(`material` / `widgets` / `cupertino`) 도 import 하지 않는다.**

예외는 `test/architecture/layer_dependency_direction_test.dart` 의 장부에 **이유와 함께** 적는다.
늘리려면 이유를 쓰고, 줄이면 총 개수도 같이 고친다. `native/` 는 플랫폼 어댑터라 **일부러 제외**돼 있다 —
여기에 방향을 새로 정하는 것은 규칙을 발명하는 것이다.

## 머지 게이트

- `flutter analyze` — **인자 없이.** `analyze lib` 은 게이트가 아니다(`test/` 를 안 본다).
- `dart run tool/affected_tests.dart` — 로컬 기본값.
  🚨**소스를 훑는 계약 테스트는 이게 못 잡는다** ⇒ UI를 추가했으면
  `app_strings_keys_test` 와 `app_shapes_coverage_test` 를 **직접** 돌린다.
- **적대검증·뮤테이션은 선택이 아니다.** 「테스트가 통과한다」는 증거가 아니다 —
  **제품 코드를 껐을 때 빨개지는지**가 증거다.
  뮤테이션은 「돌렸다」가 아니라 **「바뀐 줄을 `git diff` 로 봤다」**로 센다.
- `bash tool/merge_check.sh <PR번호>` 가 **exit 0** 일 때만 머지한다.

## UI 컨벤션

- ⛔**없다가 생기는 UI 금지.** 자리는 항상 예약하고 **내용만** 바꾼다.
- ⛔**설명 문구 금지.** 컨트롤 밑 안내 텍스트·부연 캡션을 넣지 않는다(경고 문구는 예외).
- ⛔**스크롤바를 자동으로 숨기지 않는다.** 레인 16px, 썸 최소 32px.
- 🚨**컨트롤 위에서 시작한 제스처는 그 컨트롤의 것이다. 스크롤은 그 외에서 일어난다**
  (유저 3회: 08-14 슬라이더 「절대로안하니까 다신하지마」, 08-28 버튼, 08-29 「**터치 좌표가
  버튼인데 거기서 움직였다고 스크롤이 발생하는게 심각한 버그야**」). ⛔**「손가락 얹고 스크롤」을
  걱정하지 말 것** — 실제 제스처가 아니라 가정이었다. **탭다운이 동작**이고 누름은 버튼이 맡는다.
  지키는 법은 `axis_bar_gesture.dart` — 손대기 전에 읽는다.
- 모서리는 **`AppShapes`** 만 쓴다. 선택 표시는 **색상만**.
- 툴 설정은 **툴 설정 패널**에 넣는다. 그 툴 전용 패널을 새로 만들지 않는다.

## 기록 (절대원칙)

🔜**일거리로 시작하면 보드부터 읽는다**(`http://localhost:4321`). 유저가 제출한 답과
**그 영역의 「손대기 전에」가 거기 있다** — 카드를 펼치기 전엔 안 보인다.
⛔**보드를 손으로 고치지 않는다.** 메모리 폴더의 `board.jsonl` 한 줄이 원본이고 보드는 **열 때마다**
그려진다. 사실이 생기면 **기록에 한 줄**을 적는다. 그게 곧 보드 갱신이다.
⛔**영역의 법은 카드가 갖는다.** 새 법은 `board.jsonl` 의 `{"kind":"law","tag":…}` 를 고쳐 넣는다 —
**메모리 색인에 쓰지 않는다.** 한 작업이 두 군데 적히는 순간 갈라진다.
⛔**GitHub이 아는 것은 적지 않는다** — PR·CI·머지는 서버가 `gh` 로 직접 읽는다.
⛔**말로 보고하는 것은 기록이 아니다** — 다음 세션에 남는 것은 기록뿐이다.
🔜**결정이 필요해지면 묻지 말고 보드에 올리고 다음 일로 간다**(유저: 「채팅보다 보드가 편하다」).
🚨**결정 카드는 필드로 쓴다 — `note` 는 「답할 것」 패널에 안 그려진다**(유저 08-26: 「대답칸도
없어서 뭘 말하는건지 모르겠어」). `where`(화면에서 거기까지 가는 길)·`why`(왜 막혔나)·
`options`(**2개 이상**; 라디오가 곧 이것이다, 각 `label`·`what`·`cost`)·`recommend`.
⚠️`item`+`ask` 는 아예 안 뜬다. 어기면 `board_check.exe` 가 Stop 훅에서 턴을 막는다.
⛔**미결이 남은 칸은 착수 가능이 아니다** — 갈라서 되는 부분만 남긴다.
미답 질문이 달린 카드는 **보드가 알아서 대기 중으로 내린다**(손으로 옮기지 말 것).

⚠️**이 파일만 어느 체크아웃에서든 실린다.** 워크트리에서 세션을 열면 메모리 폴더도 훅도 없다 —
**거기서도 지켜져야 하는 것만 여기 넣는다.**

## 함정 (실제로 밟은 것들)

- ⛔`git add -A` 금지. 커밋 전 **브랜치 확인.** 워크트리는 **`git -C <절대경로>`** 로만 —
  `git stash` 는 리포 전역이라 남의 작업까지 걷어간다.
- 머지하면 **본진과 워크트리 둘 다** 당긴다. 「커밋을 안 하니 깨끗하다」는 최신이라는 뜻이 아니다.
- ⛔PowerShell 일괄 치환 금지 — **한글이 깨진다.** `grep -P` 도 이 환경에서는 실패한다.
- `jq` 가 PATH에 없다. `gh --jq` 를 쓴다(파이프 `jq` 는 조용히 죽는다).
- 🚨bash 경로를 **PowerShell 따옴표 안에** 박을 땐 `pwd -W`. `pwd` 의 `/c/…` 는 PowerShell이 못 읽는데
  **exit 0을 돌려주고 아무 일도 안 한다**(인자로 넘기면 자동 변환되므로 따옴표 안에서만 터진다).
