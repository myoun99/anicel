// CocoaPods compiles Classes/ only; the real source lives in src/ shared
// with every other platform. Same pattern as the sibling forwarders.
//
// The one reader of a medium stored in a span — plain or framed — that the
// movie and sound decoders read through, and Dart's own reads of a framed
// medium too (qa_media_span.h). Without it an iPad or a Mac lacks the
// symbols and answers 「no engine」 to every compressed carried file.
#include "../../src/qa_media_span.c"
